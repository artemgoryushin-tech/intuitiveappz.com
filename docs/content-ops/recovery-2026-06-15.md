# Recovery Report - 2026-06-15

This report records preflight findings before the orchestration upgrade. It preserves recovery context without mutating content branches.

## Clean-State Notes

- Current working branch: `codex/english-version`.
- Current upstream after correction: `intuitiveappz/codex/english-version`.
- Unrelated local dirty file: `next-env.d.ts`.
- Do not stage, revert, or publish `next-env.d.ts` as part of content operations.

## Stuck Branch Candidate

Branch: `codex/auto-br-content-2026-06-12`

- Local commit: `30d800afc65be280de07e75f954daa1fd87fb673` (`Add Binance vs OKX affiliate guide`).
- Remote-tracking branch: `global21/codex/auto-br-content-2026-06-12`.
- Remote-tracking commit after fetch: `a3501c4f39546ae73f472c9a1029de91c3cc5005`.
- Difference: local branch is one content commit ahead of remote-tracking branch.
- Diff summary: `data/site.ts`, 55 changed lines.
- Preservation source: local task branch commit `30d800a`; release steward must push/verify the task branch before any cleanup.

Required release-steward action:

1. Re-check remote state before acting.
2. If the branch is still missing remote commit `30d800a`, push the task branch or mark it review-only.
3. Run source/compliance/technical gates before any publish to `global21/main`.

## Detached Worktree Candidate

Path: `~/.codex/worktrees/a8f4/DropDomain Landings 2`

- State: detached HEAD at `a3501c4`.
- Dirty file: `data/site.ts`.
- Diff summary: 55 changed lines.
- The diff matches the Binance vs OKX Brazil guide candidate also present in local branch `codex/auto-br-content-2026-06-12`.

Required release-steward action:

1. Treat as recovery evidence, not a fresh source of truth.
2. Confirm whether it is identical to commit `30d800a`.
3. Do not delete until the task branch is verified on the intended remote or explicitly archived.

## Prunable Worktree

Path reported by `git worktree list`:

- `/private/tmp/br-content-2026-06-12`
- Branch: `codex/auto-br-content-2026-06-12`
- Status: prunable; path no longer exists.

Required action:

- Safe to prune as metadata cleanup after confirming no path exists.

## Lessons For Orchestration

- Add preflight branch-router checks before content work.
- Add post-run release-steward verification for local-vs-remote branch commits.
- Record every content-changing run in `editorial-memory.md`.
- Keep source/compliance gates separate from content generation.
